# 项目复审记录（2026-09-03）

## 与参考项目的区别

| 项目 | 主要形式 | 扫描范围 | 工程化状态 |
| --- | --- | --- | --- |
| [VirtualTowel/ChaoXingDownloader](https://github.com/VirtualTowel/ChaoXingDownloader) | Python Scrapy | 资料栏目递归下载 | 依赖手填 Cookie，项目较旧，无浏览器选择界面 |
| [ColdThunder11/ChaoXingDownload](https://github.com/ColdThunder11/ChaoXingDownload) | 油猴脚本 | 页面中可见课件/媒体 | 安装轻，但依赖脚本管理器，目录和后台队列能力有限 |
| [DURUII/chaoxing-downloader](https://github.com/DURUII/chaoxing-downloader) | Python 工具 | 课程视频/资料方向 | 依赖命令行和额外 Python 包 |
| 本项目 | Manifest V3 WebExtension | 资料栏目 + 章节附件 | 有测试、CI、Release 打包、权限/隐私/贡献规范和 Agent 部署契约 |

## 当前优势

- 不要求用户复制 Cookie 或安装本地引擎。
- Chrome、Edge、Firefox 共用一套扩展核心。
- 有树状选择、下载队列、重试和目录安全处理。
- 有 GitHub Actions、预发布包、README、隐私、安全和贡献文档。
- `AGENTS.md` 定义了可机器读取的安装准备流程。

## 与用户现有仓库规范的对齐

参考 `/Users/songsong/code/dsh-bottom-info-bar`、`dsh-auto-compact`、`QzonePhoto` 和 `brickindex` 的现有约定，本仓库现在统一采用：

- `README.md` 为英文主说明，`README.zh-CN.md` 为简体中文说明。
- `AGENTS.md` 作为 Agent 入口，先读文档、检查状态、运行验证并明确未验证项。
- Conventional Commits、MIT 署名 `songoao25`、Issue/PR 模板、CODEOWNERS。
- CI、Release、CodeQL、Dependabot、安全策略、贡献指南和行为准则。
- 对外说明使用用户视角；不提交个人路径、Cookie、真实数据或内部过程流水账。
- 发布前本地测试/构建/敏感信息检查，发布后回读远程 SHA、Release 和 Actions。

## 当前不足

- 只有 Node 单元测试，尚无真实登录态的四浏览器端到端测试。
- 章节动态接口可能需要针对不同学校部署增加适配器；通用 GET/DOM 扫描不能保证覆盖所有部署。
- 当前 Firefox 方案是临时加载，正式版长期安装仍需 Mozilla 签名。
- Safari 只有 WebExtension 资源，没有可签名的 macOS App 封装。
- 免商店安装受浏览器安全策略限制，Agent 不能替用户静默确认加载。

## GitHub 行业规范复核

- GitHub About 描述改为英文默认，Homepage 仅在存在真实站点或演示页时填写，和用户其他公开仓库保持一致。
- 增加 CodeQL README 徽章、Node 版本声明和 `npm run audit:repo` 自动审计入口。
- 增加可复制的 [GitHub 仓库规范模板](repository-standard.md)，覆盖元数据、双语 README、社区文件、自动化、发布前检查和推送后回读。
- 现有社区健康文件、CI、CodeQL、Dependabot、Issue/PR 模板和 main 分支保护继续保留。

依据：GitHub 官方仓库最佳实践要求 README、许可证、贡献指南、行为准则和安全能力；Chrome 扩展发布要求 manifest、图标、短描述和根目录打包结构；Firefox 发布要求可验证的 ZIP/XPI 包。详见 README 的参考链接和仓库规范模板。

## 本次修正

- README 增加完整 English 版本，并明确区分“实现支持”和“真实验证状态”。
- Agent 浏览器自动检测增加 Linux、Windows 用户目录和 macOS 路径。
- 修正下载进度统计混入旧任务的问题，并显示失败数量。
- 保留 Alpha 标识，不把未做真实浏览器验收的能力写成正式保证。

## 验证边界

本次已执行 Node 测试、构建、商店包构建和静态检查。未执行的项目包括：真实学习通账号扫描、Chrome/Edge/Firefox/Safari 四浏览器端到端下载、Firefox AMO 签名和 Safari App 签名/公证。
